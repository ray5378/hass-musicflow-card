# i18n 国际化进度

> hass-musicflow-card（Home Assistant 前端卡片）的 i18n 需求与进度。语言方向：**中文 + English，完全跟随 HA 系统语言**。

## 整体任务需求

- 卡片全部面向用户的文案（UI 标签、按钮、空态/加载/错误提示、播放模式/进度语义标签等）接入本地化。
- **完全跟随 HA 系统语言**自动切换：HA 语言 zh 开头显示中文，其余显示英文；**不提供任何可见/手动切换入口**（无切换 UI、无 localStorage 覆盖）。
- 默认中文，未翻译语言回退中文；键集合 zh/en 保持一致。
- 与本仓库主分支服务端能力保持同步（当前 `v1.6.94`）。

## 当前进度：已完成

| 项 | 状态 |
|---|---|
| 文案盘点（面向用户的中英文硬编码） | ✅ 全部接入 `_t()`，源码仅剩注释内中文 |
| 本地化基础设施 `src/localize/localize.js` | ✅ 跟随 HA 语言，无手动切换 |
| 语言包 `languages/zh.js` / `en.js` | ✅ 62 key，zh/en 一一对齐 |
| 键对齐校验 | ✅ zh/en 无差异，卡片 `_t()` key 全部覆盖 |
| 构建产物 `dist/hass-musicflow-card.js` | ✅ 已含本地化逻辑，无 `localStorage` |
| CI（dist 最新提交 + README 纯 ASCII + HACS 校验） | 待 push 后盯 |

## 机制说明

- 卡片 `_loc()` 读取 `this._hass.language`，`localize()` 判定 `zh*` → 中文，其余 → 英文；
- 缺 key 先回退中文，仍缺则回显 key 本身；
- 语言按 `hass.language` 缓存复用，HA 切换语言时 `_loc()` 自动重建 localizer。

## 关联仓库

- 集成组件 i18n 见 `hass-musicflow/i18n进度.md`。
- 主仓库 i18n 见 `MusicFlow/i18n进度.md`。
